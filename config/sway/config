############################# Imp startup processes #############################

# Waybar
exec waybar
exec_always sleep 2 && killall -SIGUSR2 waybar

# Clipboard
exec clipse -listen

# On Screen display/indicator
exec swayosd-server

#################################################################################

include /etc/sway/config.d/*
include ~/.config/sway/config.d/*
